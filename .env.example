# ============================================================================
# TeamSpeak-Discord Bridge Configuration Template
# ============================================================================
# Copy this file to .env and replace the placeholder values with your actual credentials
# Lines without # are ready to use - just replace the placeholder values

# [Discord] settings
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE                   # Discord bot token from Discord Developer Portal
DISCORD_CHANNEL_ID=123456789012345678                       # Target Discord text channel ID (right-click channel -> Copy ID)

# [TeamSpeak ServerQuery] settings  
TS3_HOST=127.0.0.1                                          # TeamSpeak server host/IP address
TS3_VOICE_PORT=9987                                         # Virtual server voice port (usually 9987)
TS3_QUERY_PORT=10011                                        # ServerQuery port (usually 10011)
TS3_SERVERQUERY_USERNAME=serveradmin                        # ServerQuery username (usually serveradmin)
TS3_SERVERQUERY_PASSWORD=YOUR_SERVERQUERY_PASSWORD_HERE     # ServerQuery password from TS3 server
TS3_CHANNEL_ID=1                                            # Target TeamSpeak channel ID for bridging (1 = default channel)

# [Bridge] settings
TS3_NICKNAME=Bridge                                         # Nickname for the ServerQuery client connection

# [Webhook Logging] settings (optional)
# When enabled, the bridge will also send copies of bridged messages to this webhook URL.
# Messages authored by webhooks are ignored to prevent loops.
USE_WEBHOOK=False                                           # true/false (or 1/0, yes/no)
DISCORD_WEBHOOK_URL=                                        # e.g., https://discord.com/api/webhooks/XXXXXXXX/XXXXXXXXXXXXXXXX

# ============================================================================
# Configuration Notes:
# - To get Discord Channel ID: Right-click channel in Discord -> Copy ID (needs developer mode enabled)
# - To get TS3 Channel ID: View the channel information on the right panel -> Copy Channel ID  
# - ServerQuery password is set in TeamSpeak server configuration
# - Make sure Discord bot has proper permissions in target channel
# - Make sure ServerQuery user has proper permissions in TeamSpeak
# ============================================================================
